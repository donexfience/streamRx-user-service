import { Injectable } from '@nestjs/common';
import { GrpcMethod } from '@nestjs/microservices';
import { CreateUserRequest, CreateUserResponse } from '../../generated/user';

@Injectable()
export class UserService {
 
}